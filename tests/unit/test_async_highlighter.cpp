/// test_async_highlighter.cpp — Unit tests
#include "core/AsyncHighlighter.h"

#include <catch2/catch_test_macros.hpp>

using namespace markamp::core;

TEST_CASE("AsyncHighlighter: header compiles", "[async_highlighter]")
{
    REQUIRE(true);
}
