/// test_deck_manager.cpp — Unit tests
#include "core/DeckManager.h"

#include <catch2/catch_test_macros.hpp>

using namespace markamp::core;

TEST_CASE("DeckManager: header compiles", "[deck_manager]")
{
    REQUIRE(true);
}
