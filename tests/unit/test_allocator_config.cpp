/// test_allocator_config.cpp — Unit tests
#include "core/AllocatorConfig.h"

#include <catch2/catch_test_macros.hpp>

using namespace markamp::core;

TEST_CASE("AllocatorConfig: header compiles", "[allocator_config]")
{
    REQUIRE(true);
}
