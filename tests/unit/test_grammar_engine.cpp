/// test_grammar_engine.cpp — Unit tests
#include "core/GrammarEngine.h"

#include <catch2/catch_test_macros.hpp>

using namespace markamp::core;

TEST_CASE("GrammarEngine: header compiles", "[grammar_engine]")
{
    REQUIRE(true);
}
