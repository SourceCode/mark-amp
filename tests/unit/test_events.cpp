/// test_events.cpp — Unit tests
#include "core/Events.h"
#include <catch2/catch_test_macros.hpp>
using namespace markamp::core::events;

TEST_CASE("Events: header compiles", "[events]")
{
    REQUIRE(true);
}
