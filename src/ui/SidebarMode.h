#pragma once

namespace markamp::ui
{

/// Sidebar display mode — one entry per ActivityBarItem.
enum class SidebarMode
{
    kExplorer,
    kSearch,
    kSettings,
    kThemes,
    kExtensions,
    kNotebooks,
    kCanvas,
    kGraph
};

} // namespace markamp::ui
