// ScrollDecorationProvider.cpp — header-only providers, this file anchors the vtable.

#include "ScrollDecorationProvider.h"

// vtable anchor for IScrollDecorationProvider (pure virtual, defined in header).
// Explicit instantiation not needed; the derived classes provide their vtable
// through implemented overrides.
