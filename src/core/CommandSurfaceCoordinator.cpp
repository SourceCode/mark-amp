#include "CommandSurfaceCoordinator.h"

// V22 Phase 04: CommandSurfaceCoordinator
//
// All metrics use default-initialized values from the header structs.
// This .cpp exists for ABI stability and future extension (e.g., per-density
// or per-theme metric overrides).

namespace markamp::core
{

// Currently all methods are inline in the header.
// This translation unit ensures linkage and future extensibility.

} // namespace markamp::core
